<li>
	{{name}}

	<button class="change-eat" data-id="{{id}}" data-eat="{{eat}}">{{#if eat}}Devour It!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete" data-id="{{this.id}}">Delete!</button>
</li>